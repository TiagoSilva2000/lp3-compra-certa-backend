1) endpoints:
  POST /user - criar usuário
  GET /user/{id} - mostrar informações sobre usuário
  PUT /user/{id} - atualizar informações sobre usuário
  DELETE /user/{id} - soft delete no usuário

  GET /product/home - listar produtos de cinco categorias diferentes
  GET /product?queryparams - listar todos os produtos. Aceitar query params para filtragem
  GET /product/{id} - informações sobre produto

  POST /order - criar pedido
  PATCH /order/{id} - mudar status do pedido
  GET /order/{id} - visualizar pedido
  GET /order - visualizar pedidos de usuário (usar token)
  GET /order/control - visualizar pedidos
  PATCH /order/{id}/received - set pedido as received
  POST /order/{id}/rating - avaliar pedido

  POST /address - criar endereço (usar token)
  GET /address - visualizar endereços (usar token)
  GET /address/{addressId} - visualizar endereço (usar token)
  PUT /address/{addressId} - atualizar endereço (usar token)
  PATCH /address/{addressId} - tornar endereço padrão (usar token)
  DELETE /address/{addressId} - hard delete em pagamento (usar token)

  POST /payment - criar opção de pagamento
  GET /payment - visualizar opções de pagamento
  GET /payment/{payId} - visualizar opção de pagamento
  PATCH /payment/{payId} - tornar pagamento padrão
  DELETE /payment/{payId} - hard delete em pagamento

  GET /wishlist - listar itens da wishlist (usar token)
  POST /wishlist/{productId} - adicionar item a wishlist (usar token)
  DELETE /wishlist/{productId} - remover item da wishlist (usar token)


  POST /auth - fazer login
  DELETE /auth?token - finalizar sessão

  GET /dash - pegar informações de desempenho

##########################################################

DONE  


DISCARDED

  GET /user/{id}/order/{orderId} - visualizar pedido
  GET /user/{id}/order - visualizar pedidos de usuário
  PATCH /user/{id}/order/{orderId} - set pedido as received

  GET /user/{id}/wishlist - listar itens da wishlist
  POST /user/{id}/wishlist/{productId} - adicionar item a wishlist

  POST /user/{id}/payment - criar opção de pagamento
  GET /user/{id}/payment - visualizar opções de pagamento
  GET /user/{id}/payment/{payId} - visualizar opção de pagamento
  PATCH /user/{id}/payment/{payId} - tornar pagamento padrão
  DELETE /user/{id}/payment/{payId} - hard delete em pagamento

  POST /user/{id}/payment - criar opção de pagamento
  GET /user/{id}/payment - visualizar opções de pagamento
  GET /user/{id}/payment/{payId} - visualizar opção de pagamento
  PATCH /user/{id}/payment/{payId} - tornar pagamento padrão
  DELETE /user/{id}/payment/{payId} - hard delete em pagamento

